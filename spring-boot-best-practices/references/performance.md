# Spring Boot Performance Tips

- Use pagination for large queries.
- Index frequently queried database columns.
- Avoid N+1 queries (use fetch joins).
- Cache read-heavy endpoints.
- Monitor using Actuator and Micrometer.
