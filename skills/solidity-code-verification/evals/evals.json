{
  "skill_name": "solidity-code-verification",
  "evals": [
    {
      "id": 1,
      "prompt": "Audit this Solidity code for vulnerabilities:\n\ncontract Vault {\n  mapping(address => uint) balances;\n  function withdraw(uint amount) public {\n    require(balances[msg.sender] >= amount);\n    (bool success, ) = msg.sender.call{value: amount}(\"\");\n    balances[msg.sender] -= amount;\n  }\n}",
      "expected_output": "Security audit identifying the re-entrancy vulnerability with risk level and fix recommendation.",
      "assertions": [
        "Re-entrancy vulnerability is identified",
        "A risk level (High/Medium/Low) is assigned",
        "A fix or mitigation is recommended"
      ]
    },
    {
      "id": 2,
      "prompt": "Is this smart contract code secure? Check for any issues:\n\ncontract Token {\n  address public owner;\n  function setOwner(address _owner) public {\n    owner = _owner;\n  }\n}",
      "expected_output": "Security analysis identifying the access control vulnerability.",
      "assertions": [
        "Missing access control is flagged",
        "The risk of unauthorized ownership change is explained",
        "A fix recommendation is provided"
      ]
    },
    {
      "id": 3,
      "prompt": "Review this contract before I deploy it to mainnet",
      "expected_output": "Comprehensive security review with categorized findings.",
      "assertions": [
        "The response requests or analyzes the contract code",
        "Findings are categorized by severity",
        "Deployment readiness assessment is provided"
      ]
    }
  ]
}
